                                  .uef^"                                          .~uOOOOu+~.    
                ...     ..      :d88E                   .u    .     .xn!~%x.     Z8F      `8N.  
             :~""888h.:^"888:   `888E          .uuu   .d88B :@8c   x888' '888.  d88L       98E  
           X888n. 8888X  ?888>   888E~?888L :888'8888. 4888>'88"  8888l   X8888 "88888888NNu.   
             `*88 8888~ x88x.    888E  888E 8888.+"    4888>      `8888  :88888  .zf""*8888888L 
            ..<"  88*`  88888X   888E  888E 8888L     .d888L .+     `"**~ 88888 d8F      ^%888E 
             :888888H.    `%88> m888N= 888>  "88888%     "Y"      '8888>  8888~ '%N.       dl"  
               `"888888:    X"   `Y"   888     "YP'                888"  :88%     '^"====="'    
                  %888888x.-`         J88"                          ^^"==^""                      
                   '""**""            @%                                                           
                                    :"                                                             
                 <!--https://twitter.com/VeryVhersty // https://veryvhersty.tumblr.com/-->
                 
                 
                 
=========================================================================================================
`7MM"""YMM                 mm                                    `7MM    db            mm          
  MM    `7                 MM                                      MM                  MM          
  MM   d  .gP"Ya   ,6"Yb.mmMMmm `7MM  `7MM  `7Mb,od8 .gP"Ya        MM  `7MM  ,pP"Ybd mmMMmm     gp 
  MM   Y 8M""""""  ,pm9MM  MM     MM    MM    MM    8M""""""       MM    MM  `YMMMa.   MM          
.JMML.    `Mbmmd' `Moo9^Yo.`Mbmo  `Mbod"YML..JMML.   `Mbmmd'     .JMML..JMML.M9mmmP'   `Mbmo    db 
---------------------------------------------------------------------------------------------------------                 

General functions:
!!! Window Layering, based on order of activity. Selecting a window or clicking the taskbar will bring it to the top.
!!! Window draggability from holding the titlebar, click and drag to move them around.
!!! Tasks opened and currently active will react from the taskbar and the taskbar will react to window adjustments.

Programs:
Tumblr; The latest, high quality hand picked funnies.
!!! Posts are selected manually by Thea Vanherst, and generate infinately.
### Notes and date of posting found in the post header.
### Post address featured in the post address bar.
!   - Clicking "Go" will focus a singular post through a redirect.
### Posts can be reblogged if you have a Tumblr account.

Display Properties; Change the site apperance to your liking.
!!! All adjustments made through submenus are previewable through the 'Appearance' and the 'Effects' tabs.
!!! Themes can be applied by pressing the 'Apply' button, or the 'Okay' button. To cancel selected changes, press 'cancel' to close the window.
### Wallpapers:
### - Wallpapers change the current active desktop based on selected image.
### Background colours;
### - Background colours change the default background colour if no background is active, alongside span and scrollbar colour.
!   - 12 Default colours to choose from which coenside with default windows 98 options.
!   - 4 Preset colours to suit matching schemes.
### Schemes;
### - Schemes change the current desktop sprites, wallpaper, desktop colour and theme colours to chosen presets.
!   - 7 Premade custom themes in the appearance tab.
!   - 2 High constrast themes for extra accessability.

Embedded Media Player; A Fully functioning youtube embedded player.
### Media player control features
### - Play, Pause, Stop, Skip, Play next, Play previous, Shuffle alongside volume controls.
!   - Skip time feature is based on time periods of 5 seconds by default, but can decreased and increased through the 'links' dropdown menu.
!   - Shuffle & Play next or previous is based on if an imported playlist is detected, shuffle is false by default.
!   - Default Player volume is 100.
### Functional URL bar for importing based on Playlist and Video URL.
### - Accepted types of data importation; Video URL / Playlist URL / Index ID / Timecode
### - Accepted url examples: 
!   @ Video: www.youtube.com/watch?v=##########, https://youtu.be/##########, www.youtube.com/watch?v=##########&t=11
!   @ Playlist: https://www.youtube.com/playlist?list=#########################&index=1, https://www.youtube.com/watch?v=###########&list=#########################
### Grabbing current playlist &/or video URL.
!   - Exportable video URLs; Video URL / Playlist URL / Video & Playlist URL    

Patch Notes // Last updated; Know the latest features!
!!! Maximize or restore down the window to toggle between modes.
### Last updated:
!   - States the date and time the page was updated.
### Patch Notes:
!   @ Includes LIVE updates to website adjustments.
!   - Lists the updates in latest to oldest.

The Command Prompt; Export, import and force functions onto the site.
!!! Changes made through the command prompt can not be undone without a page refresh.
!   States the current active windows on the page.
!   States theme change

Internet Explorer; The ability to use the world wide web.
### N/a

Notepad; Type to your hearts content!
### N/a

=========================================================================================================   
`7MM"""Yb.                    `7MM"""Mq.                                              mm          
  MM    `Yb.                    MM   `MM.                                             MM          
  MM     `Mb  ,pW"Wq.  ,pP"Ybd  MM,,,IM9 `7Mb,od8 ,pW"Wq.`7MMpMMMb.pMMMb. `7MMpdMAo.mmMMmm     gp 
  MM     ,MP 8M     M8 `YMMMa.  MM         MM    8M     M8 MM    MM    MM   MM    M8  MM          
.JMMmmmdP'    `Ybmd9'  M9mmmP'.JMML.     .JMML.   `Ybmd9'.JMML  JMML  JMML. MMbmmd'   `Mbmo    db 
-------------------------------------------------------------------------   MM   -------------------------                 
                                                                          .JMML.                 
$console
### length               : Will calculate the current character count and state said count.
### split       + string : Splits the current string in the command prompt based on a character, limited to two strings*.

player
### video       + string : loads video based on url.
### playlist    + string : loads playlist based on url.
### push        + string : pushes video to player based on id
### reset                : Resets the command prompt to it's default state.
### fullscreen + Boolean : vhange the fullscreen status.

$custom
### console
   + background + string : changes the prompt background colour based on string insert.
         + text + string : changes the prompt text colour based on string insert.
### root        + string : enforces a root variable to an inserted value.
### var         + string : enforces a variable to an inserted value.
### refresh              : enforces a scheme refresh.

mount                    : loads the mount directory into the loaded configuration.
### local                : store the local directory into the mount directory.
       + config + string : add 'config/' to the local to the current directory.
          + img + string : add 'img/' to the local to the current directory.
       https:// + string : adds a custom url as the focused directory.
       
fetch
### mount                : splits the current config into seperate lines.

status                   : will state the currently loaded variables stored.
### config               : states the current config loaded from the load command.
### mount                : states four varables currently stored from the mount command.

flush                    : flushes all variables currently active in the prompt.
### function1            : flushes the first function if one is loaded.
### function2            : flushes the second function if one is loaded.
### function3            : flushes the third function if one is loaded.
### function4            : flushes the fourth function if one is loaded.
### config               : flushes the current config loaded.
### run                  : flushes the current running script.

get
### readme               : prints out the readme.
### url         + string : prints out the html of a loaded url.
!                        : default command to clear the 'get' command without printing.

push
### config 
          + var + string : pushes selected config as the background image.
       + readme + string : pushes selected config to the readme.
### url    
          + var + string : pushes url string to the background image.
       + readme + string : pushes selected config to the readme.

print         + interger : prints the currently mounted config based on inserted time in ms.
### default              : prints out the currently mounted based on preprogrammed times in the mounted config.

reset
### player               : resets the position of the embedded mediaplayer.
### vunny                : resets the position of the "unvunny" windows.
### display              : resets the position of the display properties and resets the current active tab.
### prompt               : resets the position of the command prompt.
### patchnotes           : focuses the patch notes and resets it to the default position.
### $inactive            : turns all windows to inactive.

clear                    : clears the current active text in the command prompt.
 
========================================================================================================= 
                                                             ,,                             ,...       
`7MM"""Yp,                `7MM                             `7MM      `7MM"""Mq.           .d' ""       
  MM    Yb                  MM                               MM        MM   `MM.          dM`          
  MM    dP  ,6"Yb.  ,p6"bo  MM  ,MP'.gP"Ya `7MMpMMMb.   ,M""bMM        MM   ,M9  .gP"Ya  mMMmm      gp 
  MM"""bg. 8)   MM 6M'  OO  MM ;Y  ,M'   Yb  MM    MM ,AP    MM        MMmmdM9  ,M'   Yb  MM        "" 
  MM    `Y  ,pm9MM 8M       MM;Mm  8M""""""  MM    MM 8MI    MM        MM  YM.  8M""""""  MM           
  MM    ,9 8M   MM YM.    , MM `Mb.YM.    ,  MM    MM `Mb    MM        MM   `Mb.YM.    ,  MM        ,, 
.JMMmmmd9  `Moo9^Yo.YMbmd'.JMML. YA.`Mbmmd'.JMML  JMML.`Wbmd"MML.    .JMML. .JMM.`Mbmmd'.JMML.      db
---------------------------------------------------------------------------------------------------------  

Global look-up documentation (Please view in RAW format):
#ref5000: changes current wallpaper background colour
#ref5001: changes taskbar text colour and active header colour if active or unactive
#ref5002: changes header background images through github directs
#ref5003: changes current theme active colours for desktop windows and store them into preview colours to be applied later through #ref0001
#ref5004: changes current header background colour from var #ref1002, and changes the current invert status for icons such as minimize, help, ect.
#ref5005: makes adjustments to different spritesheets if alternate versions are needed, usually just the same by default.
#ref5006; assigns varable to taskbar grabbing #ref1006
#ref5007: opens the task, resets window position and enables the taskbar. #ref1007
#ref5008: assigns a positional value based on how many tasks x the width of the task, and sets the position. Then determines the current state of the task.
#ref5009: checks the positional value of the current task and updates if it is visible on the taskbar.
#ref5010: inactive placement holder as there's no need to update the current position of the task that is being removed.
#ref5011: disables the task that is current being updated as inactive from the taskbar.
#ref5012: removes a task from the current amount of active tasks.
#ref5013: toggle checks to see if the current task is focused or unfocused. Only one can be active at a time.
#ref5014: variable storing of grabbing elemenets to save file space.
#ref5015: changes the default positional values based on absolute values and changes to default widths / heights.
#ref5016: When pressing on the (tab-number) tab, set zindex to highest tab.
#ref5017: resets the index of all tabs to prevent weird z-index or positional bugs.
#ref5018: changes to the current tab and adjusts height and width to overlap / prioritise the tab
#ref5019: Resets ALL potential background selection background color through #ref0019, stores the #ref selected option to a varable under #ref1015 #ref1000. 
          Changes the current active wallpaper as a variable which is pulled from github and stores it to #ref1016, 
          changes the apperance preview of said wallpaper through #ref0020.
          Finally, uses #ref1017's current selection to change the background the selected wallpaper.
#ref5020: changes monitor previews to the current wallpaper.
#ref5021: changes the appearance preview and icon preview to current wallpaper
#ref5022: boolean toggle gates for dropdown menus.
#ref5023: opens the boolean toggle dropdown menu.
#ref5024: closes the boolean toggle dropdown menu.
#ref5025: enforces closing the boolean toggle dropdown menu.
#ref5026: Variable storage for colours to be applied later.
#ref5027: Makes all other windows inactive and checks current layer and adds one to then add it through the var cal and updates it.
          Toggles the taskbar to an active position through (taskname)ButtonOpen = false. changing the taskbar to the active text (curactivetitle)
          Changes the titlebar header to active and changes margin-left from -20000 to 0.
#ref1028: Changes taskbar to active.
#ref1029: Closes the window by moving margin-left to -20000px and deactivating the taskbar.
#ref1030: Closes the window by moving margin-left to -20000px
#ref1031: Resets positional values through left and up for said window. 

